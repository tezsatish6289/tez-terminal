# Copy to `.env.local` for local dev (never commit `.env.local`).

# Required to toggle "FreedomBot public" (Live / Hidden) in simulator bot config.
# Generate: openssl rand -base64 32
PUBLIC_LIVE_PASSPHRASE=
