
{
  "entities": {
    "WebhookConfig": {
      "title": "WebhookConfig",
      "type": "object",
      "description": "Configuration for an external signal source managed by the admin.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "secretKey": { "type": "string" },
        "endpointUrl": { "type": "string" },
        "isActive": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "name", "secretKey"]
    },
    "Signal": {
      "title": "Signal",
      "type": "object",
      "description": "A trading alert/signal received from TradingView via the bridge.",
      "properties": {
        "id": { "type": "string" },
        "webhookId": { "type": "string" },
        "receivedAt": { "type": "string", "format": "date-time" },
        "symbol": { "type": "string" },
        "type": { "type": "string", "enum": ["BUY", "SELL", "NEUTRAL"] },
        "payload": { "type": "string" },
        "note": { "type": "string" }
      },
      "required": ["id", "symbol", "type", "receivedAt"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/webhooks/{webhookId}": {
      "schema": "#/backend/entities/WebhookConfig",
      "description": "Global bridge configurations managed exclusively by hello@tezterminal.com."
    },
    "/signals/{signalId}": {
      "schema": "#/backend/entities/Signal",
      "description": "The real-time feed of market alerts consumed by all traders."
    }
  }
}
