import { FnoNinjaMySubscriptionPage } from "@/components/fnoninja/FnoNinjaMySubscriptionPage";

// The FNONINJA layout provides the shell (nav + footer); this route only
// renders its content.
export default function MySubscriptionPage() {
  return <FnoNinjaMySubscriptionPage />;
}
