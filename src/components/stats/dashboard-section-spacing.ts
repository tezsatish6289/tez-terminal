/** Vertical rhythm between major dashboard folds (KPIs, monthly chart, ratios). */
export const DASHBOARD_SECTION_STACK =
  "flex flex-col gap-16 sm:gap-20 lg:gap-24";

/** Inner spacing within a single fold (cards + chart row). */
export const DASHBOARD_SECTION_INNER = "space-y-6 sm:space-y-8";
