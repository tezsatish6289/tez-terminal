/** Shared layout tokens for freedombot.ai marketing + app pages. */
export const FB_PAGE_ROOT =
  "min-h-dvh w-full overflow-x-hidden flex flex-col";

export const FB_CONTENT_SHELL = "max-w-[1200px] mx-auto w-full px-4 sm:px-6";

export const FB_WIDE_SHELL = "max-w-[1440px] mx-auto w-full px-4 sm:px-6 lg:px-8";

/** FNONINJA landing — one consistent gutter between wide data canvas and tight text. */
export const FNO_LANDING_SHELL =
  "max-w-[1360px] mx-auto w-full px-5 sm:px-7 lg:px-10";

export const FB_DOC_SHELL = "max-w-6xl mx-auto w-full px-4 sm:px-6";

export const FB_NARROW_SHELL = "max-w-3xl mx-auto w-full px-4 sm:px-6";

export const FB_MEDIUM_SHELL = "max-w-4xl mx-auto w-full px-4 sm:px-6";

/** FNONINJA learn article pages — between narrow blog and full-width data views. */
export const FNO_LEARN_ARTICLE_SHELL = "max-w-4xl mx-auto w-full px-4 sm:px-6";

/** FNONINJA learn pages with embedded live product demos (edge-to-edge canvas). */
export const FNO_LEARN_WIDE_SHELL = "w-full min-w-0 px-3 sm:px-5";

export const FB_COMPACT_SHELL = "max-w-2xl mx-auto w-full px-4 sm:px-6";

/** Hero / CTA sections on the landing page. */
export const FB_HERO_SHELL = `${FB_NARROW_SHELL}`;

/** Levels slideshow + bubbles (wide data canvas). */
export const FB_LEVELS_SHELL =
  "w-full max-w-[100rem] mx-auto px-3 sm:px-5 min-w-0";

export const FB_DASHBOARD_DETAIL_SHELL =
  "max-w-7xl mx-auto w-full px-4 sm:px-6 min-w-0";

/** Below FreedomBotNav (h-14 / sm:h-16). */
export const FB_VIEWPORT_MAIN =
  "min-h-0 flex-1 flex flex-col w-full min-w-0";

export const FB_FULL_HEIGHT_MAIN =
  "h-[calc(100dvh-3.5rem)] sm:h-[calc(100dvh-4rem)] min-h-0 overflow-hidden flex flex-col";
